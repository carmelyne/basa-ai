# 2026-05-20 — 11:48 — asset-pipeline-local-flux

## Session Summary
- What we worked on: Designing and implementing an automated image asset generation pipeline for basa-ai lesson content
- What got done:
  - Created `scripts/generate-assets.js` — auto-detects missing lesson images from `lessons.json`, generates them, and updates `lesson-data.ts` static require registry
  - Integrated local Ollama Flux (`x/flux2-klein:4b-fp4`) as primary generator (no API cost) using `/usr/bin/expect` to automate the REPL
  - Added `cwebp` conversion step (PNG → WebP) after generation
  - Gemini Imagen API as optional fallback if `GEMINI_API_KEY` is set
  - Distinct dimensions per asset type: scenario cover = **1254×1254** (1:1), word images = **1448×1086** (4:3)
  - Prompts are photorealistic style, not illustrations
  - Word images can have a custom `imagePrompt` field in `lessons.json` to override default
  - Queue is sequential (`for...of` + `await`) — GPU safe
  - Added `prestart` and `generate-assets` scripts to `package.json`
  - All changes committed and type-checked clean

- Where we stopped: All committed, clean. No active blockers.

## Next Session Needs
- Immediate next task: Add a new word/lesson to `lessons.json` to test the live pipeline trigger
- Blockers: None
- Open questions:
  - Will `expect` correctly detect the new PNG from Flux output when queue runs? (Not tested live yet, only smoke-tested the script logic)
  - Should there be a `--force` flag to regenerate existing images?

## Context to Carry
- Key decisions made:
  - Scenario covers = square 1254×1254, word assets = 4:3 landscape 1448×1086
  - Prompts use photorealistic/studio photography style, no illustrations
  - Klein-images dir is hardcoded as `/Users/carmelyne/dev/klein-images` (Flux output dir)
- Files touched:
  - `scripts/generate-assets.js` (new)
  - `src/learning/lesson-data.ts` (auto-regenerated by script)
  - `package.json` (added prestart + generate-assets scripts)
- Gotchas/warnings:
  - `sips` on macOS cannot write WebP — must use `cwebp` at `/usr/local/bin/cwebp`
  - React Native Metro requires static `require()` — lesson-data.ts is auto-generated, do NOT edit it manually
  - `GEMINI_API_KEY` is optional; if absent, goes straight to local Flux
